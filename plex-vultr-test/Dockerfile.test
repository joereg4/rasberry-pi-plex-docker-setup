FROM ubuntu:22.04

# Install basic tools
RUN apt-get update && \
    DEBIAN_FRONTEND=noninteractive apt-get install -y \
    curl \
    wget \
    git

WORKDIR /app

# Keep container running
CMD ["tail", "-f", "/dev/null"]
